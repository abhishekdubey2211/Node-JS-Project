###############################################################
# 🌐 CORS (Cross-Origin Resource Sharing) Configuration
###############################################################

# Allowed origins (comma-separated list)
# Only requests from these domains will be accepted.
CORS_ORIGINS=https://example.com,https://admin.example.com,http://localhost:3000

# Allowed HTTP methods (default: GET, POST, PUT, DELETE)
CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS

# Allowed request headers (comma-separated list)
# Defines which custom headers the client can send.
CORS_HEADERS=Content-Type,Authorization,X-Requested-With

# Exposed response headers (comma-separated list)
# Makes these headers accessible to the browser.
CORS_EXPOSED_HEADERS=X-Custom-Header,Authorization


###############################################################
# ⚙️ Application Settings
###############################################################

# Application environment (options: development, production, test)
# NODE_ENV=production

# Port number on which the server will run
PORT=5000


###############################################################
# 📝 Logging Configuration
###############################################################

# Directory where logs will be stored
# LOG_DIR=/var/logs/user-service

# Base name for log files
LOG_FILE_NAME=user-service

# Log level (options: error, warn, info, http, verbose, debug, silly)
LOG_LEVEL=debug

# Logging rotation frequency
# Options: hourly, daily, weekly, monthly, yearly
LOG_ROTATION=daily

# keep last 15 files
LOG_MAX_FILES=15       

# each file max 50MB
LOG_MAX_SIZE=50m      


# If you want milliseconds included:
LOG_TIMESTAMP_FORMAT=YYYY-MM-DD HH:mm:ss.SSS
# LOG_TIMESTAMP_FORMAT=DD-MMM-YYYY HH:mm:ss  # 25-Sep-2025 23:45:12
# LOG_TIMESTAMP_FORMAT=MMM DD, YYYY HH:mm    # Sep 25, 2025 23:45
# LOG_TIMESTAMP_FORMAT=YYYY-MM-DD hh:mm:ss A  # 12-hour clock with AM/PM  2025-09-25 11:45:12 PM

###############################################################
# 🌐 Application Settings
###############################################################

# Application environment (development | production | test)
NODE_ENV=development

# Port number
PORT=3000

###############################################################
# 🗄 Database Configuration (MySQL)
###############################################################
# Choose DB client: mysql2 | sequelize | typeorm | prisma
DB_CLIENT=typeorm   

# MySQL host
DB_HOST=localhost   # or 127.0.0.1

# MySQL port
DB_PORT=3306

# MySQL username
DB_USER=root    # root

# MySQL password
DB_PASS=abhi@22112000    # Abhi@22112000

# MySQL database name
DB_NAME=mydb

# Prisma connection string (derived from above vars)
DATABASE_URL="mysql://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}"

# Retry settings for DB connection
DB_RETRY_COUNT=5
DB_RETRY_DELAY=2   # seconds


###############################################################
# 🔐 JWT Settings
###############################################################

# JWT Secret
JWT_SECRET=supersecretkey

# JWT signing algorithm
JWT_ALGO=HS256

# JWT default expiry
JWT_EXPIRY=1h

SENSITIVE_FIELDS=password,pwd,token,auth,secret,apiKey
